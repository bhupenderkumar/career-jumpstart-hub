import React from 'react';

/**
 * PWA Install Prompt component
 * 
 * This component has been intentionally disabled as per user request.
 * It previously showed a prompt to install the web app as a PWA,
 * but now it always returns null to prevent the prompt from showing.
 */
const PWAInstallPrompt: React.FC = () => {
  // Always return null to prevent the prompt from showing
  return null;
};

export default PWAInstallPrompt;
